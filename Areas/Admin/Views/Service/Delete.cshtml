@model JollibeeClone.Models.Service

@{
    ViewData["Title"] = "XÃ³a dá»‹ch vá»¥";
}

<div class="service-delete">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="page-title">
                <i class="fas fa-trash text-danger me-2"></i>
                XÃ³a dá»‹ch vá»¥
            </h2>
            <p class="text-muted">XÃ¡c nháº­n xÃ³a dá»‹ch vá»¥ khá»i há»‡ thá»‘ng</p>
        </div>
        <div class="header-actions">
            <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>
                Quay láº¡i danh sÃ¡ch
            </a>
        </div>
    </div>

    <!-- Warning Alert -->
    <div class="alert alert-danger border-0 shadow-sm mb-4">
        <div class="d-flex align-items-center">
            <i class="fas fa-exclamation-triangle fa-2x me-3"></i>
            <div>
                <h5 class="alert-heading mb-1">Cáº£nh bÃ¡o!</h5>
                <p class="mb-0">HÃ nh Ä‘á»™ng nÃ y sáº½ xÃ³a vÄ©nh viá»…n dá»‹ch vá»¥ vÃ  khÃ´ng thá»ƒ hoÃ n tÃ¡c. Báº¡n cÃ³ cháº¯c cháº¯n muá»‘n tiáº¿p tá»¥c?</p>
            </div>
        </div>
    </div>

    <!-- Service Preview -->
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <div class="card shadow-sm border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-concierge-bell me-2"></i>
                        Dá»‹ch vá»¥ sáº½ bá»‹ xÃ³a
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        @if (!string.IsNullOrEmpty(Model.ImageUrl))
                        {
                            <div class="col-md-4">
                                <div class="service-image-preview">
                                    <img src="@Model.ImageUrl" alt="@Model.ServiceName" class="img-fluid rounded" />
                                </div>
                            </div>
                            <div class="col-md-8">
                                <div class="service-info">
                                    <h4 class="fw-bold text-dark mb-3">@Model.ServiceName</h4>
                                    
                                    @if (!string.IsNullOrEmpty(Model.ShortDescription))
                                    {
                                        <p class="text-muted mb-3">@Model.ShortDescription</p>
                                    }
                                    
                                    <div class="meta-info">
                                        <div class="row g-3">
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">ID:</label>
                                                    <div class="fw-bold">#@Model.ServiceID</div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">Tráº¡ng thÃ¡i:</label>
                                                    <div>
                                                        <span class="badge @(Model.IsActive ? "bg-success" : "bg-secondary")">
                                                            <i class="fas @(Model.IsActive ? "fa-check-circle" : "fa-pause-circle") me-1"></i>
                                                            @(Model.IsActive ? "Äang hoáº¡t Ä‘á»™ng" : "Táº¡m dá»«ng")
                                                        </span>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">Thá»© tá»± hiá»ƒn thá»‹:</label>
                                                    <div class="fw-bold">@Model.DisplayOrder</div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">CÃ³ ná»™i dung:</label>
                                                    <div class="fw-bold">
                                                        @if (!string.IsNullOrEmpty(Model.Content))
                                                        {
                                                            <span class="text-success">
                                                                <i class="fas fa-check me-1"></i>
                                                                CÃ³
                                                            </span>
                                                        }
                                                        else
                                                        {
                                                            <span class="text-muted">
                                                                <i class="fas fa-times me-1"></i>
                                                                KhÃ´ng
                                                            </span>
                                                        }
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                        else
                        {
                            <div class="col-12">
                                <div class="service-info">
                                    <h4 class="fw-bold text-dark mb-3">@Model.ServiceName</h4>
                                    
                                    @if (!string.IsNullOrEmpty(Model.ShortDescription))
                                    {
                                        <p class="text-muted mb-3">@Model.ShortDescription</p>
                                    }
                                    
                                    <div class="meta-info">
                                        <div class="row g-3">
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">ID:</label>
                                                    <div class="fw-bold">#@Model.ServiceID</div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">Tráº¡ng thÃ¡i:</label>
                                                    <div>
                                                        <span class="badge @(Model.IsActive ? "bg-success" : "bg-secondary")">
                                                            <i class="fas @(Model.IsActive ? "fa-check-circle" : "fa-pause-circle") me-1"></i>
                                                            @(Model.IsActive ? "Äang hoáº¡t Ä‘á»™ng" : "Táº¡m dá»«ng")
                                                        </span>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">Thá»© tá»± hiá»ƒn thá»‹:</label>
                                                    <div class="fw-bold">@Model.DisplayOrder</div>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="info-item">
                                                    <label class="text-muted small">CÃ³ ná»™i dung:</label>
                                                    <div class="fw-bold">
                                                        @if (!string.IsNullOrEmpty(Model.Content))
                                                        {
                                                            <span class="text-success">
                                                                <i class="fas fa-check me-1"></i>
                                                                CÃ³
                                                            </span>
                                                        }
                                                        else
                                                        {
                                                            <span class="text-muted">
                                                                <i class="fas fa-times me-1"></i>
                                                                KhÃ´ng
                                                            </span>
                                                        }
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="text-center mt-4">
                <form asp-action="Delete" method="post" class="d-inline">
                    <input asp-for="ServiceID" type="hidden" />
                    
                    <button type="submit" class="btn btn-danger btn-lg me-3" onclick="return confirmDelete()">
                        <i class="fas fa-trash me-2"></i>
                        XÃ¡c nháº­n xÃ³a
                    </button>
                </form>
                
                <a href="@Url.Action("Details", new { id = Model.ServiceID })" class="btn btn-outline-info btn-lg me-3">
                    <i class="fas fa-eye me-2"></i>
                    Xem chi tiáº¿t
                </a>
                
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary btn-lg">
                    <i class="fas fa-times me-2"></i>
                    Há»§y bá»
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete() {
            var serviceName = @Html.Raw(Json.Serialize(Model.ServiceName));
            return confirm('Báº¡n cÃ³ cháº¯c cháº¯n muá»‘n xÃ³a dá»‹ch vá»¥ "' + serviceName + '"?\n\nHÃ nh Ä‘á»™ng nÃ y khÃ´ng thá»ƒ hoÃ n tÃ¡c!');
        }
        
        // Auto focus on delete button
        $(document).ready(function() {
            $('button[type="submit"]').focus();
        });
    </script>
}

@section Styles {
    <style>
        .service-image-preview {
            max-height: 200px;
            overflow: hidden;
            border-radius: 8px;
        }
        
        .service-image-preview img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .info-item {
            padding: 8px 0;
        }
        
        .info-item label {
            display: block;
            margin-bottom: 4px;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .meta-info {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-top: 15px;
        }
        
        .alert-danger {
            background: linear-gradient(135deg, #f8d7da 0%, #f5c6cb 100%);
            color: #721c24;
        }
        
        .card {
            transition: all 0.3s ease;
        }
        
        .btn-lg {
            padding: 12px 30px;
            font-size: 16px;
        }
        
        .service-info h4 {
            line-height: 1.4;
        }
    </style>
} 

