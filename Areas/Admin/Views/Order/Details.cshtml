@model JollibeeClone.Models.Orders

@{
    ViewData["Title"] = "Chi tiáº¿t Ä‘Æ¡n hÃ ng";
}

@section Styles {
    <link rel="stylesheet" href="~/css/order-admin.css" asp-append-version="true" />
}

<div class="order-details">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="page-title">
                <i class="fas fa-receipt text-jollibee-red me-2"></i>
                Chi tiáº¿t Ä‘Æ¡n hÃ ng #@Model.OrderCode
            </h2>
            <p class="text-muted">ThÃ´ng tin chi tiáº¿t vá» Ä‘Æ¡n hÃ ng</p>
        </div>
        <div class="header-actions">
            @if (Model.OrderStatusID != 5 && Model.OrderStatusID != 6) // KhÃ´ng pháº£i Ä‘Ã£ giao hoáº·c Ä‘Ã£ há»§y
            {
                <a href="@Url.Action("UpdateStatus", new { id = Model.OrderID })" class="btn btn-warning me-2">
                    <i class="fas fa-edit me-2"></i>
                    Cáº­p nháº­t tráº¡ng thÃ¡i
                </a>
            }
            <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>
                Quay láº¡i
            </a>
        </div>
    </div>

    <div class="row">
        <!-- ThÃ´ng tin Ä‘Æ¡n hÃ ng -->
        <div class="col-lg-8">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        ThÃ´ng tin Ä‘Æ¡n hÃ ng
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><strong>MÃ£ Ä‘Æ¡n hÃ ng:</strong></label>
                                <div class="form-control-plaintext">
                                    <span class="badge bg-primary fs-6">#@Model.OrderCode</span>
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label"><strong>NgÃ y Ä‘áº·t hÃ ng:</strong></label>
                                <div class="form-control-plaintext">
                                    @Model.OrderDate.ToString("dd/MM/yyyy HH:mm")
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label"><strong>Tráº¡ng thÃ¡i:</strong></label>
                                <div class="form-control-plaintext">
                                    @{
                                        string statusClass = "secondary";
                                        string statusIcon = "fas fa-clock";
                                        switch (Model.OrderStatusID)
                                        {
                                            case 1: // Chá» xÃ¡c nháº­n
                                                statusClass = "warning";
                                                statusIcon = "fas fa-clock";
                                                break;
                                            case 2: // ÄÃ£ xÃ¡c nháº­n
                                                statusClass = "info";
                                                statusIcon = "fas fa-check-circle";
                                                break;
                                            case 3: // Äang chuáº©n bá»‹
                                                statusClass = "primary";
                                                statusIcon = "fas fa-utensils";
                                                break;
                                            case 4: // Äang giao
                                                statusClass = "warning";
                                                statusIcon = "fas fa-shipping-fast";
                                                break;
                                            case 5: // ÄÃ£ giao
                                                statusClass = "success";
                                                statusIcon = "fas fa-check-double";
                                                break;
                                            case 6: // ÄÃ£ há»§y
                                                statusClass = "danger";
                                                statusIcon = "fas fa-times-circle";
                                                break;
                                        }
                                    }
                                    <span class="badge bg-@statusClass fs-6">
                                        <i class="@statusIcon me-1"></i>
                                        @Model.OrderStatus?.StatusName
                                    </span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label"><strong>PhÆ°Æ¡ng thá»©c thanh toÃ¡n:</strong></label>
                                <div class="form-control-plaintext">
                                    <i class="fas fa-credit-card me-2"></i>
                                    @Model.PaymentMethod?.MethodName
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label"><strong>PhÆ°Æ¡ng thá»©c giao hÃ ng:</strong></label>
                                <div class="form-control-plaintext">
                                    <i class="fas fa-shipping-fast me-2"></i>
                                    @Model.DeliveryMethod?.MethodName
                                </div>
                            </div>
                            @if (!string.IsNullOrEmpty(Model.NotesByCustomer))
                            {
                                <div class="mb-3">
                                    <label class="form-label"><strong>Ghi chÃº tá»« khÃ¡ch hÃ ng:</strong></label>
                                    <div class="form-control-plaintext">
                                        <div class="alert alert-light border">
                                            <i class="fas fa-sticky-note me-2"></i>
                                            @Model.NotesByCustomer
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>

            <!-- Sáº£n pháº©m trong Ä‘Æ¡n hÃ ng -->
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-shopping-bag me-2"></i>
                        Sáº£n pháº©m Ä‘Ã£ Ä‘áº·t (@Model.OrderItems?.Count() sáº£n pháº©m)
                    </h5>
                </div>
                <div class="card-body">
                    @if (Model.OrderItems != null && Model.OrderItems.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead class="table-light">
                                    <tr>
                                        <th>Sáº£n pháº©m</th>
                                        <th class="text-center">Sá»‘ lÆ°á»£ng</th>
                                        <th class="text-end">ÄÆ¡n giÃ¡</th>
                                        <th class="text-end">ThÃ nh tiá»n</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.OrderItems)
                                    {
                                        <tr>
                                            <td>
                                                <div class="d-flex align-items-center">
                                                    <div class="product-image me-3">
                                                        <i class="fas fa-hamburger fa-2x text-jollibee-red"></i>
                                                    </div>
                                                    <div>
                                                        <h6 class="mb-1">@item.ProductNameSnapshot</h6>
                                                        @if (!string.IsNullOrEmpty(item.SelectedConfigurationSnapshot))
                                                        {
                                                            <small class="text-muted">
                                                                <i class="fas fa-cogs me-1"></i>
                                                                TÃ¹y chá»‰nh: @item.SelectedConfigurationSnapshot
                                                            </small>
                                                        }
                                                    </div>
                                                </div>
                                            </td>
                                            <td class="text-center">
                                                <span class="badge bg-light text-dark border">
                                                    @item.Quantity
                                                </span>
                                            </td>
                                            <td class="text-end">
                                                @item.UnitPrice.ToString("N0") â‚«
                                            </td>
                                            <td class="text-end">
                                                <strong>@item.Subtotal.ToString("N0") â‚«</strong>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <i class="fas fa-shopping-bag fa-3x text-muted mb-3"></i>
                            <p class="text-muted">KhÃ´ng cÃ³ sáº£n pháº©m nÃ o trong Ä‘Æ¡n hÃ ng</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- ThÃ´ng tin khÃ¡ch hÃ ng vÃ  tÃ³m táº¯t -->
        <div class="col-lg-4">
            <!-- ThÃ´ng tin khÃ¡ch hÃ ng -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-user me-2"></i>
                        ThÃ´ng tin khÃ¡ch hÃ ng
                    </h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label"><strong>Há» vÃ  tÃªn:</strong></label>
                        <div class="form-control-plaintext">
                            <i class="fas fa-user-circle me-2"></i>
                            @Model.CustomerFullName
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><strong>Email:</strong></label>
                        <div class="form-control-plaintext">
                            <i class="fas fa-envelope me-2"></i>
                            <a href="mailto:@Model.CustomerEmail">@Model.CustomerEmail</a>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label"><strong>Sá»‘ Ä‘iá»‡n thoáº¡i:</strong></label>
                        <div class="form-control-plaintext">
                            <i class="fas fa-phone me-2"></i>
                            <a href="tel:@Model.CustomerPhoneNumber">@Model.CustomerPhoneNumber</a>
                        </div>
                    </div>
                    @if (Model.UserAddress != null)
                    {
                        <div class="mb-3">
                            <label class="form-label"><strong>Äá»‹a chá»‰ giao hÃ ng:</strong></label>
                            <div class="form-control-plaintext">
                                <i class="fas fa-map-marker-alt me-2"></i>
                                @Model.UserAddress.Address
                            </div>
                        </div>
                    }
                </div>
            </div>

            <!-- TÃ³m táº¯t Ä‘Æ¡n hÃ ng -->
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-calculator me-2"></i>
                        TÃ³m táº¯t Ä‘Æ¡n hÃ ng
                    </h5>
                </div>
                <div class="card-body">
                    <div class="order-summary">
                        <div class="d-flex justify-content-between mb-2">
                            <span>Táº¡m tÃ­nh:</span>
                            <span>@Model.SubtotalAmount.ToString("N0") â‚«</span>
                        </div>
                        @if (Model.ShippingFee > 0)
                        {
                            <div class="d-flex justify-content-between mb-2">
                                <span>PhÃ­ giao hÃ ng:</span>
                                <span>@Model.ShippingFee.ToString("N0") â‚«</span>
                            </div>
                        }
                        @if (Model.DiscountAmount > 0)
                        {
                            <div class="d-flex justify-content-between mb-2 text-success">
                                <span>
                                    <i class="fas fa-tag me-1"></i>
                                    Giáº£m giÃ¡:
                                </span>
                                <span>-@Model.DiscountAmount.ToString("N0") â‚«</span>
                            </div>
                        }
                        <hr>
                        <div class="d-flex justify-content-between total-amount">
                            <strong>Tá»•ng cá»™ng:</strong>
                            <strong class="text-success fs-5">@Model.TotalAmount.ToString("N0") â‚«</strong>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 

