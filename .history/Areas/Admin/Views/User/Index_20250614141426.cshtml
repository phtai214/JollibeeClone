@model JollibeeClone.Models.PaginatedList<JollibeeClone.Models.User>

@{
    ViewData["Title"] = "Quản lý người dùng";
}

@section Styles {
    <link rel="stylesheet" href="~/css/user-admin.css" />
}

<div class="user-management">
    <!-- Header với nút thêm mới -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="page-title">
                <i class="fas fa-users text-jollibee-red me-2"></i>
                Quản lý người dùng
            </h2>
            <p class="text-muted" style="margin-bottom: 2px;">Quản lý tất cả người dùng trong hệ thống</p>
        </div>
        <div class="header-actions">
            <a href="@Url.Action("Create")" class="btn btn-jollibee btn-lg">
                <i class="fas fa-plus me-2"></i>
                Thêm người dùng mới
            </a>
        </div>
    </div>

    <!-- Hiển thị thông báo -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            <i class="fas fa-check-circle me-2"></i>
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            <i class="fas fa-exclamation-circle me-2"></i>
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Tìm kiếm và bộ lọc -->
    <form method="get" id="filterForm">
        <div class="card mb-4">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4">
                        <div class="search-box">
                            <i class="fas fa-search search-icon"></i>
                            <input type="text" name="searchString" value="@ViewBag.CurrentSearch" class="form-control" placeholder="Tìm kiếm tên, email, số điện thoại...">
                        </div>
                    </div>
                    <div class="col-md-2">
                        <select name="isActive" class="form-select">
                            <option value="">Tất cả trạng thái</option>
                            @if (ViewBag.CurrentActive?.ToString() == "True")
                            {
                                <option value="true" selected="selected">Äang hoáº¡t Ä‘á»™ng</option>
                            }
                            else
                            {
                                <option value="true">Đang hoạt động</option>
                            }
                            @if (ViewBag.CurrentActive?.ToString() == "False")
                            {
                                <option value="false" selected="selected">ÄÃ£ vÃ´ hiá»‡u hÃ³a</option>
                            }
                            else
                            {
                                <option value="false">ÄÃ£ vÃ´ hiá»‡u hÃ³a</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-2">
                        <select name="sortOrder" class="form-select">
                            @if (ViewBag.CurrentSort == null || ViewBag.CurrentSort == "")
                            {
                                <option value="" selected="selected">tên A-Z</option>
                            }
                            else
                            {
                                <option value="">tên A-Z</option>
                            }
                            @if (ViewBag.CurrentSort == "name_desc")
                            {
                                <option value="name_desc" selected="selected">tên Z-A</option>
                            }
                            else
                            {
                                <option value="name_desc">tên Z-A</option>
                            }
                            @if (ViewBag.CurrentSort == "email")
                            {
                                <option value="email" selected="selected">Email A-Z</option>
                            }
                            else
                            {
                                <option value="email">Email A-Z</option>
                            }
                            @if (ViewBag.CurrentSort == "email_desc")
                            {
                                <option value="email_desc" selected="selected">Email Z-A</option>
                            }
                            else
                            {
                                <option value="email_desc">Email Z-A</option>
                            }
                            @if (ViewBag.CurrentSort == "status")
                            {
                                <option value="status" selected="selected">Tráº¡ng thÃ¡i</option>
                            }
                            else
                            {
                                <option value="status">Tráº¡ng thÃ¡i</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-3">
                        <button type="submit" class="btn btn-secondary me-2">
                            <i class="fas fa-search"></i> Lá»c
                        </button>
                        <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                            <i class="fas fa-times"></i> XÃ³a bá»™ lá»c
                        </a>
                    </div>
                    <div class="col-md-1">
                        <div class="view-toggle-group">
                            <button type="button" class="btn btn-outline-secondary view-btn active" data-view="grid" title="Dáº¡ng lÆ°á»›i">
                                <i class="fas fa-th"></i>
                            </button>
                            <button type="button" class="btn btn-outline-secondary view-btn" data-view="list" title="Dáº¡ng danh sÃ¡ch">
                                <i class="fas fa-list"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>

    <!-- ThÃ´ng tin káº¿t quáº£ -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div class="result-count">
            Hiá»ƒn thá»‹ @((Model.PageIndex - 1) * Model.PageSize + 1) - @(Math.Min(Model.PageIndex * Model.PageSize, Model.TotalCount)) cá»§a @Model.TotalCount ngÆ°á»i dÃ¹ng
        </div>
        <div class="pagination-info">
            Trang @Model.PageIndex / @Model.TotalPages
        </div>
    </div>

    <!-- Danh sÃ¡ch ngÆ°á»i dÃ¹ng -->
    @if (Model.Any())
    {
        <!-- Grid View -->
        <div id="gridView" class="view-container">
            <div class="row">
                @foreach (var user in Model)
                {
                    <div class="col-lg-3 col-md-4 col-sm-6 mb-4 user-item">
                        <div class="modern-user-card">
                            <!-- Card Header -->
                            <div class="card-header-section">
                                <div class="user-avatar-modern">
                                    <span class="avatar-text">@user.FullName.Substring(0, 1)</span>
                                    <div class="status-indicator @(user.IsActive ? "status-online" : "status-offline")"></div>
                                </div>
                                <div class="quick-actions">
                                    <div class="dropdown">
                                        <button class="btn btn-link dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                            <i class="fas fa-ellipsis-h"></i>
                                        </button>
                                        <ul class="dropdown-menu">
                                            <li><a class="dropdown-item" href="@Url.Action("Details", new { id = user.UserID })">
                                                <i class="fas fa-eye me-2"></i>Xem chi tiáº¿t
                                            </a></li>
                                            <li><a class="dropdown-item" href="@Url.Action("Edit", new { id = user.UserID })">
                                                <i class="fas fa-edit me-2"></i>Chá»‰nh sá»­a
                                            </a></li>
                                            <li><hr class="dropdown-divider"></li>
                                            <li><a class="dropdown-item text-danger" href="@Url.Action("Delete", new { id = user.UserID })">
                                                <i class="fas fa-trash me-2"></i>XÃ³a
                                            </a></li>
                                        </ul>
                                    </div>
                                </div>
                            </div>

                            <!-- Card Body -->
                            <div class="card-body-section">
                                <div class="user-identity">
                                    <h5 class="user-name">@user.FullName</h5>
                                    <span class="user-id">#@user.UserID</span>
                                </div>

                                <div class="contact-info">
                                    <div class="info-item">
                                        <i class="fas fa-envelope icon"></i>
                                        <span class="info-text" title="@user.Email">@(user.Email.Length > 25 ? user.Email.Substring(0, 25) + "..." : user.Email)</span>
                                    </div>
                                    @if (!string.IsNullOrEmpty(user.PhoneNumber))
                                    {
                                        <div class="info-item">
                                            <i class="fas fa-phone icon"></i>
                                            <span class="info-text">@user.PhoneNumber</span>
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="info-item">
                                            <i class="fas fa-phone icon text-muted"></i>
                                            <span class="info-text text-muted">ChÆ°a cÃ³ SÄT</span>
                                        </div>
                                    }
                                </div>
                            </div>

                            <!-- Card Footer -->
                            <div class="card-footer-section">
                                <div class="status-section">
                                    <span class="status-badge-modern @(user.IsActive ? "status-active-modern" : "status-inactive-modern")">
                                        <i class="fas @(user.IsActive ? "fa-check-circle" : "fa-ban") me-1"></i>
                                        @(user.IsActive ? "Hoáº¡t Ä‘á»™ng" : "VÃ´ hiá»‡u hÃ³a")
                                    </span>
                                </div>
                                <div class="action-buttons">
                                    <a href="@Url.Action("Details", new { id = user.UserID })" class="btn-action btn-view" title="Xem chi tiáº¿t">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a href="@Url.Action("Edit", new { id = user.UserID })" class="btn-action btn-edit" title="Chá»‰nh sá»­a">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    @* <a href="@Url.Action("Delete", new { id = user.UserID })" class="btn-action btn-delete" title="XÃ³a">
                                        <i class="fas fa-trash"></i>
                                    </a> *@
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

        <!-- List View -->
        <div id="listView" class="view-container" style="display: none;">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th width="80">Avatar</th>
                            <th>ThÃ´ng tin ngÆ°á»i dÃ¹ng</th>
                            <th width="140">LiÃªn há»‡</th>
                            <th width="100">Tráº¡ng thÃ¡i</th>
                            <th width="100">ÄÆ¡n hÃ ng</th>
                            <th width="120">Thao tÃ¡c</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr class="user-row">
                                <td>
                                    <div class="table-user-avatar">
                                        @user.FullName.Substring(0, 1)
                                    </div>
                                </td>
                                <td>
                                    <div class="user-info">
                                        <h6 class="mb-1">@user.FullName</h6>
                                        <small class="text-muted">
                                            <i class="fas fa-id-card me-1"></i>
                                            ID: #@user.UserID
                                        </small>
                                        <br>
                                        <small class="text-muted">
                                            <i class="fas fa-envelope me-1"></i>
                                            @user.Email
                                        </small>
                                    </div>
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(user.PhoneNumber))
                                    {
                                        <a href="tel:@user.PhoneNumber" class="btn btn-sm btn-outline-success" title="Gá»i Ä‘iá»‡n">
                                            <i class="fas fa-phone me-1"></i>
                                            @user.PhoneNumber
                                        </a>
                                    }
                                    else
                                    {
                                        <span class="text-muted small">
                                            <i class="fas fa-phone-slash me-1"></i>
                                            ChÆ°a cÃ³ SÄT
                                        </span>
                                    }
                                </td>
                                <td>
                                    @if (user.IsActive)
                                    {
                                        <span class="badge bg-success">
                                            <i class="fas fa-check me-1"></i>
                                            Hoáº¡t Ä‘á»™ng
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">
                                            <i class="fas fa-ban me-1"></i>
                                            VÃ´ hiá»‡u hÃ³a
                                        </span>
                                    }
                                </td>
                                <td>
                                    @if (user.Orders.Any())
                                    {
                                        <span class="badge bg-info">
                                            <i class="fas fa-shopping-cart me-1"></i>
                                            @user.Orders.Count
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">
                                            <i class="fas fa-shopping-cart me-1"></i>
                                            0
                                        </span>
                                    }
                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a href="@Url.Action("Details", new { id = user.UserID })" 
                                           class="btn btn-sm btn-outline-info" title="Chi tiáº¿t">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="@Url.Action("Edit", new { id = user.UserID })" 
                                           class="btn btn-sm btn-outline-warning" title="Chá»‰nh sá»­a">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a href="@Url.Action("Delete", new { id = user.UserID })" 
                                           class="btn btn-sm btn-outline-danger" title="XÃ³a">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Pagination -->
        @if (Model.TotalPages > 1)
        {
            <nav aria-label="User pagination" class="mt-4">
                <ul class="pagination justify-content-center">
                    @if (Model.HasPreviousPage)
                    {
                        <li class="page-item">
                            <a class="page-link" href="@Url.Action("Index", new { 
                                page = Model.PageIndex - 1, 
                                searchString = ViewBag.CurrentSearch, 
                                isActive = ViewBag.CurrentActive,
                                sortOrder = ViewBag.CurrentSort
                            })">
                                <i class="fas fa-chevron-left"></i>
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="page-item disabled">
                            <span class="page-link">
                                <i class="fas fa-chevron-left"></i>
                            </span>
                        </li>
                    }

                    @for (int i = Math.Max(1, Model.PageIndex - 2); i <= Math.Min(Model.TotalPages, Model.PageIndex + 2); i++)
                    {
                        @if (i == Model.PageIndex)
                        {
                            <li class="page-item active">
                                <span class="page-link">@i</span>
                            </li>
                        }
                        else
                        {
                            <li class="page-item">
                                <a class="page-link" href="@Url.Action("Index", new { 
                                    page = i, 
                                    searchString = ViewBag.CurrentSearch, 
                                    isActive = ViewBag.CurrentActive,
                                    sortOrder = ViewBag.CurrentSort
                                })">@i</a>
                            </li>
                        }
                    }

                    @if (Model.HasNextPage)
                    {
                        <li class="page-item">
                            <a class="page-link" href="@Url.Action("Index", new { 
                                page = Model.PageIndex + 1, 
                                searchString = ViewBag.CurrentSearch, 
                                isActive = ViewBag.CurrentActive,
                                sortOrder = ViewBag.CurrentSort
                            })">
                                <i class="fas fa-chevron-right"></i>
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="page-item disabled">
                            <span class="page-link">
                                <i class="fas fa-chevron-right"></i>
                            </span>
                        </li>
                    }
                </ul>
            </nav>
        }
    }
    else
    {
        <!-- Empty State -->
        <div class="empty-state">
            <i class="fas fa-users"></i>
            <h3>KhÃ´ng tÃ¬m tháº¥y ngÆ°á»i dÃ¹ng nÃ o</h3>
            <p>Thá»­ thay Ä‘á»•i bá»™ lá»c hoáº·c tÃ¬m kiáº¿m Ä‘á»ƒ hiá»ƒn thá»‹ káº¿t quáº£ khÃ¡c.</p>
            <a href="@Url.Action("Create")" class="btn btn-jollibee">
                <i class="fas fa-plus me-2"></i>
                Thêm ngÆ°á»i dÃ¹ng Ä‘áº§u tiÃªn
            </a>
        </div>
    }
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // View toggle functionality
            $('.view-btn').click(function() {
                var view = $(this).data('view');
                
                $('.view-btn').removeClass('active');
                $(this).addClass('active');
                
                if (view === 'grid') {
                    $('#gridView').show();
                    $('#listView').hide();
                } else {
                    $('#gridView').hide();
                    $('#listView').show();
                }
                
                // Save preference to localStorage
                localStorage.setItem('userViewPreference', view);
            });
            
            // Load saved view preference
            var savedView = localStorage.getItem('userViewPreference');
            if (savedView) {
                $('.view-btn[data-view="' + savedView + '"]').click();
            }
            
            // Auto-submit form when filters change
            $('select[name="isActive"], select[name="sortOrder"]').change(function() {
                $('#filterForm').submit();
            });
            
            // Search with debounce
            var searchTimeout;
            $('input[name="searchString"]').on('input', function() {
                clearTimeout(searchTimeout);
                var form = $(this).closest('form');
                searchTimeout = setTimeout(function() {
                    form.submit();
                }, 500);
            });
        });
    </script>
} 

