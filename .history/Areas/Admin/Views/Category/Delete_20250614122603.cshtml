@model JollibeeClone.Models.Categories

@{
    ViewData["Title"] = "Xóa danh mục";
}

@section Styles {
    <link rel="stylesheet" href="~/css/category-admin.css" asp-append-version="true" />
}

<div class="delete-category">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="page-title">
                <i class="fas fa-trash text-danger me-2"></i>
                Xóa danh mục
            </h2>
            <p class="text-muted">Xác nhận xóa danh mục: <strong class="text-danger">@Model.CategoryName</strong></p>
        </div>
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>
            Quay lại danh sách
        </a>
    </div>

    <!-- Cảnh báo -->
    <div class="alert alert-danger" role="alert">
        <i class="fas fa-exclamation-triangle me-2"></i>
        <strong>Cảnh báo!</strong> Hành động này không thể hoàn tác. Danh mục sẽ bị xóa vĩnh viễn khỏi hệ thống.
    </div>

    <!-- Kiểm tra ràng buộc -->
    @if ((Model.Products != null && Model.Products.Any()) || (Model.SubCategories != null && Model.SubCategories.Any()))
    {
        <div class="alert alert-warning" role="alert">
            <i class="fas fa-ban me-2"></i>
            <strong>Không thể xóa!</strong> Danh mục này có chứa:
            <ul class="mb-0 mt-2">
                @if (Model.Products != null && Model.Products.Any())
                {
                    <li>@Model.Products.Count() sản phẩm</li>
                }
                @if (Model.SubCategories != null && Model.SubCategories.Any())
                {
                    <li>@Model.SubCategories.Count() danh mục con</li>
                }
            </ul>
            <p class="mt-2 mb-0">Vui lòng chuyển hoặc xóa các mục này trước khi xóa danh mục.</p>
        </div>
    }

    <!-- Thông tin danh mục sẽ bị xóa -->
    <div class="row">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header bg-light">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        Thông tin danh mục sẽ bị xóa
                    </h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-3">ID danh mục:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.CategoryID)</dd>
                        <dt class="col-sm-3">Tên danh mục:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.CategoryName)</dd>

                        @if (!string.IsNullOrEmpty(Model.Description))
                        {
                            <dt class="col-sm-3">Mô tả:</dt>
                            <dd class="col-sm-9">@Html.DisplayFor(model => model.Description)</dd>
                        }

                        <dt class="col-sm-3">Danh mục cha:</dt>
                        <dd class="col-sm-9">
                            @if (Model.ParentCategoryID.HasValue && Model.ParentCategory != null)
                            {
                                <span class="badge bg-secondary">@Html.DisplayFor(model => model.ParentCategory.CategoryName)</span>
                            }
                            else
                            {
                                <span class="text-muted">Danh mục gốc</span>
                            }
                        </dd>

                        <dt class="col-sm-3">Sá»‘ thá»© tá»±:</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.DisplayOrder)</dd>

                        <dt class="col-sm-3">Sá»‘ sáº£n pháº©m:</dt>
                        <dd class="col-sm-9">
                            <span class="badge bg-info">@(Model.Products?.Count() ?? 0) sáº£n pháº©m</span>
                        </dd>

                        <dt class="col-sm-3">Sá»‘ danh má»¥c con:</dt>
                        <dd class="col-sm-9">
                            <span class="badge bg-secondary">@(Model.SubCategories?.Count() ?? 0) danh má»¥c con</span>
                        </dd>
                    </dl>

                    <hr>

                    <!-- Form xÃ¡c nháº­n xÃ³a -->
                    @if ((Model.Products == null || !Model.Products.Any()) && (Model.SubCategories == null || !Model.SubCategories.Any()))
                    {
                        <form asp-action="Delete" method="post">
                            <input asp-for="CategoryID" type="hidden" />
                            @Html.AntiForgeryToken()
                            
                            <div class="delete-confirmation mb-4">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="confirmDelete" required>
                                    <label class="form-check-label" for="confirmDelete">
                                        TÃ´i hiá»ƒu ráº±ng hÃ nh Ä‘á»™ng nÃ y khÃ´ng thá»ƒ hoÃ n tÃ¡c vÃ  Ä‘á»“ng Ã½ xÃ³a danh má»¥c nÃ y.
                                    </label>
                                </div>
                            </div>

                            <div class="form-actions">
                                <button type="submit" class="btn btn-danger btn-lg" id="deleteBtn" disabled>
                                    <i class="fas fa-trash me-2"></i>
                                    XÃ¡c nháº­n xÃ³a danh má»¥c
                                </button>
                                <a href="@Url.Action("Index")" class="btn btn-secondary btn-lg">
                                    <i class="fas fa-times me-2"></i>
                                    Há»§y
                                </a>
                                <a href="@Url.Action("Edit", new { id = Model.CategoryID })" class="btn btn-warning btn-lg">
                                    <i class="fas fa-edit me-2"></i>
                                    Chá»‰nh sá»­a thay vÃ¬ xÃ³a
                                </a>
                            </div>
                        </form>
                    }
                    else
                    {
                        <div class="text-center">
                            <p class="text-danger mb-3">
                                <i class="fas fa-ban me-2"></i>
                                KhÃ´ng thá»ƒ xÃ³a danh má»¥c nÃ y vÃ¬ cÃ³ chá»©a dá»¯ liá»‡u.
                            </p>
                            <div class="form-actions">
                                <a href="@Url.Action("Index")" class="btn btn-secondary btn-lg">
                                    <i class="fas fa-arrow-left me-2"></i>
                                    Quay láº¡i danh sÃ¡ch
                                </a>
                                <a href="@Url.Action("Edit", new { id = Model.CategoryID })" class="btn btn-warning btn-lg">
                                    <i class="fas fa-edit me-2"></i>
                                    Chá»‰nh sá»­a danh má»¥c
                                </a>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <!-- Danh sÃ¡ch sáº£n pháº©m trong danh má»¥c -->
            @if (Model.Products != null && Model.Products.Any())
            {
                <div class="card mt-4">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-hamburger me-2"></i>
                            Sáº£n pháº©m trong danh má»¥c (@Model.Products.Count())
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="text-warning mb-3">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            CÃ¡c sáº£n pháº©m nÃ y sáº½ cáº§n Ä‘Æ°á»£c chuyá»ƒn sang danh má»¥c khÃ¡c trÆ°á»›c khi xÃ³a:
                        </p>
                        <div class="table-responsive">
                            <table class="table table-sm">
                                <thead>
                                    <tr>
                                        <th>TÃªn sáº£n pháº©m</th>
                                        <th>GiÃ¡</th>
                                        <th>Thao tÃ¡c</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var product in Model.Products.Take(5))
                                    {
                                        <tr>
                                            <td>@product.ProductName</td>
                                            <td>@product.Price.ToString("C0", new System.Globalization.CultureInfo("vi-VN"))</td>
                                            <td>
                                                <a href="@Url.Action("Edit", "Product", new { id = product.ProductID })" class="btn btn-sm btn-outline-warning">
                                                    Chuyá»ƒn danh má»¥c
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        @if (Model.Products.Count() > 5)
                        {
                            <p class="text-muted">... vÃ  @(Model.Products.Count() - 5) sáº£n pháº©m khÃ¡c</p>
                        }
                    </div>
                </div>
            }

            <!-- Danh sÃ¡ch danh má»¥c con -->
            @if (Model.SubCategories != null && Model.SubCategories.Any())
            {
                <div class="card mt-4">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-sitemap me-2"></i>
                            Danh má»¥c con (@Model.SubCategories.Count())
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="text-warning mb-3">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            CÃ¡c danh má»¥c con nÃ y cáº§n Ä‘Æ°á»£c xá»­ lÃ½ trÆ°á»›c:
                        </p>
                        <div class="row">
                            @foreach (var subCategory in Model.SubCategories)
                            {
                                <div class="col-md-6 mb-3">
                                    <div class="sub-category-item">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <h6 class="mb-0">@subCategory.CategoryName</h6>
                                            <div>
                                                <a href="@Url.Action("Edit", new { id = subCategory.CategoryID })" class="btn btn-sm btn-outline-warning">
                                                    Chá»‰nh sá»­a
                                                </a>
                                                <a href="@Url.Action("Delete", new { id = subCategory.CategoryID })" class="btn btn-sm btn-outline-danger">
                                                    XÃ³a
                                                </a>
                                            </div>
                                        </div>
                                        @if (!string.IsNullOrEmpty(subCategory.Description))
                                        {
                                            <p class="text-muted small mb-0 mt-2">@subCategory.Description</p>
                                        }
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>

        <div class="col-lg-4">
            <!-- áº¢nh danh má»¥c -->
            <div class="card">
                <div class="card-header bg-light">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-image me-2"></i>
                        HÃ¬nh áº£nh danh má»¥c
                    </h5>
                </div>
                <div class="card-body text-center">
                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <img src="@Model.ImageUrl" alt="@Model.CategoryName" class="img-fluid rounded shadow-sm" style="max-height: 250px;" />
                    }
                    else
                    {
                        <div class="no-image">
                            <i class="fas fa-folder" style="font-size: 4rem; color: #ddd;"></i>
                            <p class="text-muted mt-2">KhÃ´ng cÃ³ áº£nh</p>
                        </div>
                    }
                </div>
            </div>

            <!-- Cáº£nh bÃ¡o liÃªn quan -->
            <div class="card mt-4">
                <div class="card-header bg-warning text-dark">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        LÆ°u Ã½ quan trá»ng
                    </h5>
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li>Danh má»¥c sáº½ bá»‹ xÃ³a vÄ©nh viá»…n khá»i há»‡ thá»‘ng</li>
                        <li>HÃ¬nh áº£nh liÃªn quan cÅ©ng sáº½ bá»‹ xÃ³a</li>
                        <li>KhÃ´ng thá»ƒ xÃ³a náº¿u cÃ³ sáº£n pháº©m hoáº·c danh má»¥c con</li>
                        <li>ThÃ´ng tin thá»‘ng kÃª sáº½ bá»‹ áº£nh hÆ°á»Ÿng</li>
                        <li>HÃ nh Ä‘á»™ng nÃ y khÃ´ng thá»ƒ hoÃ n tÃ¡c</li>
                    </ul>
                </div>
            </div>

            <!-- Gá»£i Ã½ thay tháº¿ -->
            <div class="card mt-4">
                <div class="card-header bg-info text-white">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-lightbulb me-2"></i>
                        Gá»£i Ã½ thay tháº¿
                    </h5>
                </div>
                <div class="card-body">
                    <p><strong>Thay vÃ¬ xÃ³a, báº¡n cÃ³ thá»ƒ:</strong></p>
                    <ul class="mb-0">
                        <li>Chuyá»ƒn sáº£n pháº©m sang danh má»¥c khÃ¡c</li>
                        <li>Gá»™p danh má»¥c con vÃ o danh má»¥c cha</li>
                        <li>Chá»‰nh sá»­a thÃ´ng tin Ä‘á»ƒ phÃ¹ há»£p hÆ¡n</li>
                        <li>Thay Ä‘á»•i thá»© tá»± hiá»ƒn thá»‹</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
$(document).ready(function() {
    // Enable/disable delete button based on confirmation checkbox
    $('#confirmDelete').on('change', function() {
        $('#deleteBtn').prop('disabled', !this.checked);
    });

    // Confirmation before actual deletion
    $('form').on('submit', function(e) {
        if (!$('#confirmDelete').is(':checked')) {
            e.preventDefault();
            alert('Vui lÃ²ng xÃ¡c nháº­n báº±ng cÃ¡ch Ä‘Ã¡nh dáº¥u vÃ o Ã´ checkbox!');
            return false;
        }

        var confirmed = confirm('Báº¡n cÃ³ cháº¯c cháº¯n muá»‘n xÃ³a danh má»¥c "' + '@Model.CategoryName' + '"?\n\nHÃ nh Ä‘á»™ng nÃ y khÃ´ng thá»ƒ hoÃ n tÃ¡c!');
        if (!confirmed) {
            e.preventDefault();
            return false;
        }

        // Show loading state
        var submitBtn = $('#deleteBtn');
        var originalText = submitBtn.html();
        submitBtn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-2"></i>Äang xÃ³a...');
        
        // Don't reset on timeout since this is deletion
    });
});
</script>
} 

