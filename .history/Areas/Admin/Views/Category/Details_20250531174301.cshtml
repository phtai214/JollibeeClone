@model JollibeeClone.Areas.Admin.Models.Categories

@{
    ViewData["Title"] = "Chi tiết danh mục";
}

@section Styles {
    <link rel="stylesheet" href="~/css/category-admin.css" asp-append-version="true" />
}

<div class="category-details">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="page-title">
                <i class="fas fa-folder text-jollibee-yellow me-2"></i>
                Chi tiết danh mục
            </h2>
            <p class="text-muted">Thông tin chi tiết về danh mục: <strong>@Model.CategoryName</strong></p>
        </div>
        <div class="header-actions">
            <a href="@Url.Action("Edit", new { id = Model.CategoryID })" class="btn btn-warning">
                <i class="fas fa-edit me-2"></i>
                Chỉnh sửa
            </a>
            <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>
                Quay lại danh sách
            </a>
        </div>
    </div>

    <div class="row">
        <!-- Thông tin chính -->
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        Thông tin danh mục
                    </h5>
                </div>
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-3">ID danh mục:</dt>
                        <dd class="col-sm-9">
                            <span class="badge bg-primary">#@Html.DisplayFor(model => model.CategoryID)</span>
                        </dd>

                        <dt class="col-sm-3">Tên danh mục:</dt>
                        <dd class="col-sm-9">
                            <h5 class="mb-0">@Html.DisplayFor(model => model.CategoryName)</h5>
                        </dd>

                        @if (!string.IsNullOrEmpty(Model.Description))
                        {
                            <dt class="col-sm-3">Mô tả:</dt>
                            <dd class="col-sm-9">@Html.DisplayFor(model => model.Description)</dd>
                        }

                        <dt class="col-sm-3">Danh mục cha:</dt>
                        <dd class="col-sm-9">
                            @if (Model.ParentCategoryID.HasValue && Model.ParentCategory != null)
                            {
                                <span class="badge bg-secondary fs-6">
                                    <i class="fas fa-folder me-1"></i>
                                    @Html.DisplayFor(model => model.ParentCategory.CategoryName)
                                </span>
                            }
                            else
                            {
                                <span class="text-muted">Danh mục gốc</span>
                            }
                        </dd>

                        <dt class="col-sm-3">Số thứ tự:</dt>
                        <dd class="col-sm-9">
                            <span class="badge bg-info">@Html.DisplayFor(model => model.SortOrder)</span>
                        </dd>
                    </dl>
                </div>
            </div>

            <!-- Danh mục con -->
            @if (Model.SubCategories != null && Model.SubCategories.Any())
            {
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-sitemap me-2"></i>
                            Danh mục con (@Model.SubCategories.Count())
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            @foreach (var subCategory in Model.SubCategories.OrderBy(c => c.SortOrder))
                            {
                                <div class="col-md-6 col-lg-4 mb-3">
                                    <div class="sub-category-item">
                                        <div class="sub-category-header">
                                            <h6>@subCategory.CategoryName</h6>
                                            <span class="badge bg-secondary">#@subCategory.CategoryID</span>
                                        </div>
                                        @if (!string.IsNullOrEmpty(subCategory.Description))
                                        {
                                            <p class="sub-category-desc">@subCategory.Description</p>
                                        }
                                        <div class="sub-category-actions">
                                            <a href="@Url.Action("Details", new { id = subCategory.CategoryID })" class="btn btn-sm btn-outline-info">
                                                Chi tiết
                                            </a>
                                            <a href="@Url.Action("Edit", new { id = subCategory.CategoryID })" class="btn btn-sm btn-outline-warning">
                                                Sửa
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }

            <!-- Sản phẩm trong danh mục -->
            @if (Model.Products != null && Model.Products.Any())
            {
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-hamburger me-2"></i>
                            Sản phẩm trong danh mục (@Model.Products.Count())
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Ảnh</th>
                                        <th>Tên sản phẩm</th>
                                        <th>Giá</th>
                                        <th>Trạng thái</th>
                                        <th>Thao tác</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var product in Model.Products.Take(10))
                                    {
                                        <tr>
                                            <td>
                                                @if (!string.IsNullOrEmpty(product.ImageUrl))
                                                {
                                                    <div class="table-category-image">
                                                        <img src="@product.ImageUrl" alt="@product.ProductName" />
                                                    </div>
                                                }
                                                else
                                                {
                                                    <div class="no-image-small">
                                                        <i class="fas fa-hamburger"></i>
                                                    </div>
                                                }
                                            </td>
                                            <td>@product.ProductName</td>
                                            <td>@product.Price.ToString("C0", new System.Globalization.CultureInfo("vi-VN"))</td>
                                            <td>
                                                @if (product.IsAvailable)
                                                {
                                                    <span class="badge bg-success">Có sẵn</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-warning">Hết hàng</span>
                                                }
                                            </td>
                                            <td>
                                                <a href="@Url.Action("Details", "Product", new { id = product.ProductID })" class="btn btn-sm btn-outline-info">
                                                    Chi tiết
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        @if (Model.Products.Count() > 10)
                        {
                            <div class="text-center mt-3">
                                <a href="@Url.Action("Index", "Product", new { categoryId = Model.CategoryID })" class="btn btn-outline-primary">
                                    Xem tất cả @Model.Products.Count() sản phẩm
                                </a>
                            </div>
                        }
                    </div>
                </div>
            }

            <!-- Actions -->
            <div class="card mt-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-cogs me-2"></i>
                        Thao tác
                    </h5>
                </div>
                <div class="card-body">
                    <div class="d-flex flex-wrap gap-2">
                        <a href="@Url.Action("Edit", new { id = Model.CategoryID })" class="btn btn-warning">
                            <i class="fas fa-edit me-2"></i>
                            Chỉnh sửa danh mục
                        </a>
                        <a href="@Url.Action("Delete", new { id = Model.CategoryID })" class="btn btn-danger">
                            <i class="fas fa-trash me-2"></i>
                            Xóa danh mục
                        </a>
                        <a href="@Url.Action("Create", new { parentId = Model.CategoryID })" class="btn btn-success">
                            <i class="fas fa-plus me-2"></i>
                            Thêm danh mục con
                        </a>
                        <button class="btn btn-info" onclick="copyCategoryInfo()">
                            <i class="fas fa-copy me-2"></i>
                            Sao chép thông tin
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Sidebar -->
        <div class="col-lg-4">
            <!-- Ảnh danh mục -->
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-image me-2"></i>
                        Hình ảnh danh mục
                    </h5>
                </div>
                <div class="card-body text-center">
                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <img src="@Model.ImageUrl" alt="@Model.CategoryName" class="img-fluid rounded shadow" style="max-height: 300px;" />
                        <div class="mt-2">
                            <small class="text-muted">Nhấp để xem ảnh lớn</small>
                        </div>
                    }
                    else
                    {
                        <div class="no-image">
                            <i class="fas fa-folder" style="font-size: 5rem; color: #ddd;"></i>
                            <p class="text-muted mt-3">Chưa có ảnh danh mục</p>
                            <a href="@Url.Action("Edit", new { id = Model.CategoryID })" class="btn btn-outline-primary btn-sm">
                                <i class="fas fa-plus me-1"></i>
                                Thêm ảnh
                            </a>
                        </div>
                    }
                </div>
            </div>

            <!-- Thống kê nhanh -->
            <div class="card mt-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-chart-bar me-2"></i>
                        Thống kê nhanh
                    </h5>
                </div>
                <div class="card-body">
                    <div class="stat-item">
                        <div class="stat-icon bg-primary">
                            <i class="fas fa-hamburger"></i>
                        </div>
                        <div class="stat-content">
                            <h6>@(Model.Products?.Count() ?? 0) Sản phẩm</h6>
                            <p>Trong danh mục này</p>
                        </div>
                    </div>

                    @if (Model.SubCategories != null && Model.SubCategories.Any())
                    {
                        <div class="stat-item">
                            <div class="stat-icon bg-info">
                                <i class="fas fa-sitemap"></i>
                            </div>
                            <div class="stat-content">
                                <h6>@Model.SubCategories.Count() Danh mục con</h6>
                                <p>Phân cấp bên dưới</p>
                            </div>
                        </div>
                    }

                    <div class="stat-item">
                        <div class="stat-icon bg-success">
                            <i class="fas fa-sort-numeric-down"></i>
                        </div>
                        <div class="stat-content">
                            <h6>Thứ tự: @Model.SortOrder</h6>
                            <p>Vị trí hiển thị</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Gợi ý -->
            <div class="card mt-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-lightbulb me-2"></i>
                        Gợi ý cải thiện
                    </h5>
                </div>
                <div class="card-body">
                    <ul class="improvement-tips">
                        @if (string.IsNullOrEmpty(Model.ImageUrl))
                        {
                            <li><i class="fas fa-image text-warning"></i> Thêm ảnh danh mục để trải nghiệm tốt hơn</li>
                        }
                        @if (string.IsNullOrEmpty(Model.Description))
                        {
                            <li><i class="fas fa-pencil-alt text-info"></i> Thêm mô tả giúp khách hàng hiểu rõ hơn</li>
                        }
                        @if (Model.Products == null || !Model.Products.Any())
                        {
                            <li><i class="fas fa-plus text-success"></i> Thêm sản phẩm vào danh mục</li>
                        }
                        @if (Model.SubCategories == null || !Model.SubCategories.Any())
                        {
                            <li><i class="fas fa-sitemap text-primary"></i> Tạo danh mục con để tổ chức tốt hơn</li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.sub-category-item {
    border: 1px solid #eee;
    border-radius: 8px;
    padding: 15px;
    height: 100%;
    transition: all 0.3s ease;
}

.sub-category-item:hover {
    border-color: var(--jollibee-red);
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.sub-category-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
}

.sub-category-header h6 {
    margin: 0;
    color: var(--jollibee-dark);
}

.sub-category-desc {
    color: #6c757d;
    font-size: 0.9rem;
    margin-bottom: 15px;
    line-height: 1.4;
}

.sub-category-actions {
    text-align: center;
}

.sub-category-actions .btn {
    margin: 0 2px;
}
</style>

@section Scripts {
<script>
function copyCategoryInfo() {
    var categoryInfo = `Danh mục: @Model.CategoryName
ID: @Model.CategoryID
@(!string.IsNullOrEmpty(Model.Description) ? "Mô tả: " + Model.Description : "")
@(Model.ParentCategory != null ? "Danh mục cha: " + Model.ParentCategory.CategoryName : "Danh mục gốc")
Số sản phẩm: @(Model.Products?.Count() ?? 0)`;
    
    navigator.clipboard.writeText(categoryInfo).then(function() {
        alert('Đã sao chép thông tin danh mục!');
    }).catch(function() {
        // Fallback for older browsers
        var textArea = document.createElement('textarea');
        textArea.value = categoryInfo;
        document.body.appendChild(textArea);
        textArea.select();
        document.execCommand('copy');
        document.body.removeChild(textArea);
        alert('Đã sao chép thông tin danh mục!');
    });
}

$(document).ready(function() {
    // Click to view large image
    @if (!string.IsNullOrEmpty(Model.ImageUrl))
    {
        <text>
        $('img[alt="@Model.CategoryName"]').on('click', function() {
            var modal = `
                <div class="modal fade" id="imageModal" tabindex="-1">
                    <div class="modal-dialog modal-lg">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">@Model.CategoryName</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body text-center">
                                <img src="@Model.ImageUrl" class="img-fluid" alt="@Model.CategoryName">
                            </div>
                        </div>
                    </div>
                </div>`;
            
            $('body').append(modal);
            $('#imageModal').modal('show');
            
            $('#imageModal').on('hidden.bs.modal', function() {
                $(this).remove();
            });
        });
        </text>
    }
});
</script>
} 